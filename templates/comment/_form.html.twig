{{ form_start(form,{"action":path("app_comment_new",{id:app.request.get("id")})}) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary">{{ button_label|default('Ajouter') }}</button>
{{ form_end(form) }}
